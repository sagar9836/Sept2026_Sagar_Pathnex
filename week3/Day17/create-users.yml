---
- name: Create multiple Pathnex users
  hosts: all
  become: true

  vars:
    users:
      - dev1
      - dev2
      - dev3

  tasks:
    - name: Create users
      ansible.builtin.user:
        name: "{{ item }}"
        state: present
      loop: "{{ users }}"
